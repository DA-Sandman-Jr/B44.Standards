namespace GameName;

/// <summary>
/// Delete this once the Core has real content. It exists so a freshly created
/// repository builds and tests green before any game code is written, which
/// makes the first real commit a diff about the game rather than about
/// scaffolding.
/// </summary>
public static class Placeholder
{
    public const string Name = "GameName";
}
