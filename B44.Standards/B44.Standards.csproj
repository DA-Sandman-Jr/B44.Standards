<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- netstandard2.0: the config/analyzer-carrier convention — restorable
         by every consumer TFM (net6 through net10). No lib ships anyway. -->
    <TargetFramework>netstandard2.0</TargetFramework>
    <!-- No source ships; latest just keeps the repo-wide Nullable=enable
         from tripping netstandard2.0's C# 7.3 default. -->
    <LangVersion>latest</LangVersion>
    <PackageId>B44.Standards</PackageId>
    <Version>0.10.2</Version>
    <Authors>DA-Sandman-Jr</Authors>
    <Company>B44 Labs</Company>
    <Description>B44 studio policy as a package: analyzers, severity globalconfig, code metrics, engine-free-core enforcement, and opt-in synchronized agent guidance. Reference with PrivateAssets=all.</Description>
    <RepositoryUrl>https://github.com/DA-Sandman-Jr/B44.Standards</RepositoryUrl>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <!-- All rights reserved is not an SPDX identifier, so the licence
         travels as a bundled file rather than an expression. Also stops
         nuget.org warning "License missing" on every publish. -->
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <Copyright>Copyright (c) 2026 David Sanders (B44 Labs). All rights reserved.</Copyright>
    <!-- Policy-only package: no compiled library, no content in lib/. -->
    <IncludeBuildOutput>false</IncludeBuildOutput>
    <DevelopmentDependency>true</DevelopmentDependency>
    <NoWarn>$(NoWarn);NU5128</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <!-- Plain dependencies (NOT PrivateAssets) so consumers get the analyzers
         transitively — package props must not manufacture PackageReferences. -->
    <PackageReference Include="Meziantou.Analyzer" Version="3.0.138" />
    <PackageReference Include="Microsoft.CodeAnalysis.BannedApiAnalyzers" Version="5.6.0" />
  </ItemGroup>

  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="\" />
    <None Include="../LICENSE" Pack="true" PackagePath="\" />
    <None Include="buildTransitive\**" Pack="true" PackagePath="buildTransitive" />
    <None Include="config\**" Pack="true" PackagePath="config" />
    <None Include="guidance\**" Pack="true" PackagePath="guidance" />
  </ItemGroup>
</Project>
