<Project>
  <!-- Public/network-facing projects opt into the complete SDK Security rule
       category. Pin to the project's target-framework rule level so a newer
       installed SDK cannot silently expand enforcement. Consumers can set
       B44SecurityAnalysisLevel explicitly when they need a different pin. -->
  <PropertyGroup Condition="'$(B44SecuritySensitive)' == 'true'">
    <B44SecurityAnalysisLevel Condition="'$(B44SecurityAnalysisLevel)' == '' and '$(TargetFrameworkIdentifier)' == '.NETCoreApp' and '$(TargetFrameworkVersion)' != ''">$([System.String]::Copy('$(TargetFrameworkVersion)').Substring(1))-all</B44SecurityAnalysisLevel>
    <B44SecurityAnalysisLevel Condition="'$(B44SecurityAnalysisLevel)' == ''">latest-all</B44SecurityAnalysisLevel>
    <AnalysisLevelSecurity Condition="'$(AnalysisLevelSecurity)' == ''">$(B44SecurityAnalysisLevel)</AnalysisLevelSecurity>
  </PropertyGroup>

  <!-- Layered enforcement, opt-in per project. The boundary layer (engine-free
       and deterministic authority) lives in B44.Boundaries.targets; the
       per-project verification-integrity checks live in
       B44.ProjectPolicy.targets; the repository-wide hygiene and suppression
       ratchets live in B44.RepositoryHygiene.targets. Each is imported below
       and documents its own opt-in properties.

         <B44EngineFreeCore>true</B44EngineFreeCore>       engine-free Core
         <B44EngineFree>true</B44EngineFree>               engine-free project
         <B44Deterministic>true</B44Deterministic>         no ambient time/RNG
         <B44SecuritySensitive>true</B44SecuritySensitive> public/network code:
             enables every built-in SDK Security rule at a target-level pin
             (net8.0 => 8.0-all, net10.0 => 10.0-all). -->
  <Import Project="$(MSBuildThisFileDirectory)B44.Boundaries.targets" />
  <Import Project="$(MSBuildThisFileDirectory)B44.ProjectPolicy.targets" />
  <Import Project="$(MSBuildThisFileDirectory)B44.RepositoryHygiene.targets" />

  <!-- Agent guidance is deliberately opt-in. B44.Standards can therefore be
       used outside B44-owned repositories without rewriting a consumer's
       source tree. Set the four B44Agent* properties in Directory.Build.props
       to enable it for one explicitly selected anchor project. -->
  <PropertyGroup>
    <B44AgentSyncVerifyOnly Condition="'$(B44AgentSyncVerifyOnly)' == ''">false</B44AgentSyncVerifyOnly>
    <B44AgentSyncProjectFullPath Condition="'$(B44AgentSyncProject)' != ''">$([MSBuild]::NormalizePath('$(B44AgentSyncProject)'))</B44AgentSyncProjectFullPath>
    <B44GameCoreProjectFullPath Condition="'$(B44GameCoreProject)' != ''">$([MSBuild]::NormalizePath('$(B44GameCoreProject)'))</B44GameCoreProjectFullPath>
  </PropertyGroup>

  <UsingTask
    TaskName="B44SyncAgentGuidanceTask"
    TaskFactory="RoslynCodeTaskFactory"
    AssemblyFile="$(MSBuildToolsPath)/Microsoft.Build.Tasks.Core.dll">
    <ParameterGroup>
      <RepositoryRoot ParameterType="System.String" Required="true" />
      <GuidanceProfile ParameterType="System.String" Required="true" />
      <OrganizationGuidancePath ParameterType="System.String" Required="true" />
      <GameGuidancePath ParameterType="System.String" Required="true" />
      <ToolingGuidancePath ParameterType="System.String" Required="true" />
      <ExcludedPaths ParameterType="Microsoft.Build.Framework.ITaskItem[]" Required="false" />
      <VerifyOnly ParameterType="System.Boolean" Required="true" />
    </ParameterGroup>
    <Task>
      <Code Type="Fragment" Language="cs">
        <![CDATA[
        const string organizationStart = "<!-- B44 ORGANIZATION GUIDANCE: START -->";
        const string organizationEnd = "<!-- B44 ORGANIZATION GUIDANCE: END -->";
        const string gameStart = "<!-- B44 GAME GUIDANCE: START -->";
        const string gameEnd = "<!-- B44 GAME GUIDANCE: END -->";

        var pathComparison = System.IO.Path.DirectorySeparatorChar == '\\'
            ? System.StringComparison.OrdinalIgnoreCase
            : System.StringComparison.Ordinal;

        string NormalizeDirectoryPath(string path)
        {
            var fullPath = System.IO.Path.GetFullPath(path);
            var pathRoot = System.IO.Path.GetPathRoot(fullPath);
            return string.Equals(fullPath, pathRoot, pathComparison)
                ? fullPath
                : fullPath.TrimEnd(System.IO.Path.DirectorySeparatorChar, System.IO.Path.AltDirectorySeparatorChar);
        }

        bool IsAtOrBelow(string candidate, string ancestor)
        {
            if (string.Equals(candidate, ancestor, pathComparison)) return true;
            if (candidate.Length <= ancestor.Length || !candidate.StartsWith(ancestor, pathComparison)) return false;
            var boundary = candidate[ancestor.Length];
            return boundary == System.IO.Path.DirectorySeparatorChar ||
                boundary == System.IO.Path.AltDirectorySeparatorChar;
        }

        var root = NormalizeDirectoryPath(RepositoryRoot);
        var profile = (GuidanceProfile ?? string.Empty).Trim();
        var includeGame = string.Equals(profile, "Game", System.StringComparison.OrdinalIgnoreCase);
        if (!includeGame && !string.Equals(profile, "Organization", System.StringComparison.OrdinalIgnoreCase))
        {
            Log.LogError("B44AgentGuidanceProfile must be Organization or Game when B44AgentSyncEnabled=true; received '" + profile + "'.");
            return false;
        }

        if (!System.IO.Directory.Exists(root))
        {
            Log.LogError("B44AgentRepositoryRoot does not exist: " + root);
            return false;
        }

        string ReadUtf8(string path, out bool hasBom)
        {
            var bytes = System.IO.File.ReadAllBytes(path);
            hasBom = bytes.Length >= 3 && bytes[0] == 0xEF && bytes[1] == 0xBB && bytes[2] == 0xBF;
            if ((bytes.Length >= 2 && bytes[0] == 0xFF && bytes[1] == 0xFE) ||
                (bytes.Length >= 2 && bytes[0] == 0xFE && bytes[1] == 0xFF))
            {
                throw new System.IO.InvalidDataException("Agent guidance files must use UTF-8, not UTF-16: " + path);
            }

            var offset = hasBom ? 3 : 0;
            return new System.Text.UTF8Encoding(false, true).GetString(bytes, offset, bytes.Length - offset);
        }

        void WriteUtf8(string path, string content, bool withBom)
        {
            var directory = System.IO.Path.GetDirectoryName(path);
            if (!string.IsNullOrEmpty(directory)) System.IO.Directory.CreateDirectory(directory);
            var payload = new System.Text.UTF8Encoding(false).GetBytes(content);
            if (!withBom)
            {
                System.IO.File.WriteAllBytes(path, payload);
                return;
            }

            var bytes = new byte[payload.Length + 3];
            bytes[0] = 0xEF;
            bytes[1] = 0xBB;
            bytes[2] = 0xBF;
            System.Buffer.BlockCopy(payload, 0, bytes, 3, payload.Length);
            System.IO.File.WriteAllBytes(path, bytes);
        }

        string NewlineFor(string text)
        {
            return text.IndexOf("\r\n", System.StringComparison.Ordinal) >= 0 ? "\r\n" : "\n";
        }

        string NormalizeFragment(string text, string newline)
        {
            return text.Replace("\r\n", "\n").Replace("\r", "\n")
                .TrimEnd('\n')
                .Replace("\n", newline);
        }

        int Count(string text, string value)
        {
            var count = 0;
            var index = 0;
            while ((index = text.IndexOf(value, index, System.StringComparison.Ordinal)) >= 0)
            {
                count++;
                index += value.Length;
            }
            return count;
        }

        string UpsertBlock(string document, string start, string end, string body, string newline, string afterMarker)
        {
            var startCount = Count(document, start);
            var endCount = Count(document, end);
            if (startCount != endCount || startCount > 1)
            {
                throw new System.IO.InvalidDataException(
                    "Managed guidance markers are malformed or duplicated: " + start + " / " + end + ".");
            }

            var block = start + newline + body + newline + end;
            if (startCount == 1)
            {
                var pattern = System.Text.RegularExpressions.Regex.Escape(start) + ".*?" +
                    System.Text.RegularExpressions.Regex.Escape(end);
                return System.Text.RegularExpressions.Regex.Replace(
                    document,
                    pattern,
                    _ => block,
                    System.Text.RegularExpressions.RegexOptions.Singleline);
            }

            if (!string.IsNullOrEmpty(afterMarker))
            {
                var markerIndex = document.IndexOf(afterMarker, System.StringComparison.Ordinal);
                if (markerIndex < 0)
                {
                    throw new System.IO.InvalidDataException("Cannot place managed game guidance because the organization block is absent.");
                }
                var insertAt = markerIndex + afterMarker.Length;
                return document.Insert(insertAt, newline + newline + block);
            }

            var firstLineBreak = document.IndexOf('\n');
            var headingEnd = firstLineBreak < 0 ? document.Length : firstLineBreak + 1;
            var before = document.Substring(0, headingEnd).TrimEnd('\r', '\n');
            var after = document.Substring(headingEnd).TrimStart('\r', '\n');
            return before + newline + newline + block + newline + newline + after;
        }

        string RemoveBlock(string document, string start, string end, string newline)
        {
            var startCount = Count(document, start);
            var endCount = Count(document, end);
            if (startCount != endCount || startCount > 1)
            {
                throw new System.IO.InvalidDataException(
                    "Managed guidance markers are malformed or duplicated: " + start + " / " + end + ".");
            }
            if (startCount == 0) return document;

            var pattern = "(?:\\r?\\n){0,2}" + System.Text.RegularExpressions.Regex.Escape(start) + ".*?" +
                System.Text.RegularExpressions.Regex.Escape(end) + "(?:\\r?\\n){0,2}";
            return System.Text.RegularExpressions.Regex.Replace(
                document,
                pattern,
                _ => newline + newline,
                System.Text.RegularExpressions.RegexOptions.Singleline);
        }

        bool WriteOrVerify(string path, string expected, bool expectedBom, string description)
        {
            if (System.IO.File.Exists(path))
            {
                bool existingBom;
                var existing = ReadUtf8(path, out existingBom);
                if (existing == expected && existingBom == expectedBom) return true;
            }

            if (VerifyOnly)
            {
                Log.LogError(description + " is stale or missing: " + path);
                return false;
            }

            WriteUtf8(path, expected, expectedBom);
            Log.LogMessage(Microsoft.Build.Framework.MessageImportance.High, "Synced " + path);
            return true;
        }

        try
        {
            var excludedDirectoryNames = new System.Collections.Generic.HashSet<string>(
                new[]
                {
                    ".git", ".godot", ".vs", ".b44", ".venv",
                    "bin", "obj", "TestResults", "node_modules", "artifacts",
                    "dist", "coverage", "publish"
                },
                System.StringComparer.OrdinalIgnoreCase);
            var excludedRoots = new System.Collections.Generic.List<string>();
            foreach (var item in ExcludedPaths ?? System.Array.Empty<Microsoft.Build.Framework.ITaskItem>())
            {
                var configuredPath = (item.ItemSpec ?? string.Empty).Trim();
                if (configuredPath.Length == 0) continue;

                var excludedRoot = NormalizeDirectoryPath(
                    System.IO.Path.IsPathRooted(configuredPath)
                        ? configuredPath
                        : System.IO.Path.Combine(root, configuredPath));
                if (string.Equals(excludedRoot, root, pathComparison))
                {
                    Log.LogError("B44AgentSyncExclude cannot exclude the repository root: " + configuredPath);
                    return false;
                }
                if (!IsAtOrBelow(excludedRoot, root))
                {
                    Log.LogError("B44AgentSyncExclude must stay inside B44AgentRepositoryRoot. Received: " + configuredPath);
                    return false;
                }
                if (System.IO.File.Exists(excludedRoot))
                {
                    Log.LogError("B44AgentSyncExclude accepts directories, not files: " + configuredPath);
                    return false;
                }
                if (!excludedRoots.Exists(existing => string.Equals(existing, excludedRoot, pathComparison)))
                {
                    excludedRoots.Add(excludedRoot);
                }
            }

            var rootClaude = System.IO.Path.Combine(root, "CLAUDE.md");
            if (!System.IO.File.Exists(rootClaude))
            {
                Log.LogError("Agent guidance synchronization requires a root CLAUDE.md: " + rootClaude);
                return false;
            }

            bool rootBom;
            var rootText = ReadUtf8(rootClaude, out rootBom);
            var newline = NewlineFor(rootText);

            bool ignoredBom;
            var organizationBody = NormalizeFragment(ReadUtf8(OrganizationGuidancePath, out ignoredBom), newline);
            var gameBody = NormalizeFragment(ReadUtf8(GameGuidancePath, out ignoredBom), newline);
            var toolingBody = NormalizeFragment(ReadUtf8(ToolingGuidancePath, out ignoredBom), newline) + newline;

            var updatedRoot = UpsertBlock(
                rootText, organizationStart, organizationEnd, organizationBody, newline, null);
            updatedRoot = includeGame
                ? UpsertBlock(updatedRoot, gameStart, gameEnd, gameBody, newline, organizationEnd)
                : RemoveBlock(updatedRoot, gameStart, gameEnd, newline);

            var success = WriteOrVerify(rootClaude, updatedRoot, rootBom, "Root CLAUDE.md managed guidance");
            var toolingDestination = System.IO.Path.Combine(root, ".b44", "B44.Tooling.md");
            success = WriteOrVerify(toolingDestination, toolingBody, false, "Generated B44 tooling guidance") && success;

            var claudeFiles = new System.Collections.Generic.List<string>();
            var agentsFiles = new System.Collections.Generic.List<string>();
            var pending = new System.Collections.Generic.Stack<string>();
            pending.Push(root);
            while (pending.Count > 0)
            {
                var directory = pending.Pop();
                claudeFiles.AddRange(System.IO.Directory.GetFiles(directory, "CLAUDE.md", System.IO.SearchOption.TopDirectoryOnly));
                agentsFiles.AddRange(System.IO.Directory.GetFiles(directory, "AGENTS.md", System.IO.SearchOption.TopDirectoryOnly));
                foreach (var child in System.IO.Directory.GetDirectories(directory))
                {
                    var name = System.IO.Path.GetFileName(child);
                    var normalizedChild = NormalizeDirectoryPath(child);
                    var isReparsePoint = (System.IO.File.GetAttributes(child) & System.IO.FileAttributes.ReparsePoint) != 0;
                    var isConfiguredExclude = excludedRoots.Exists(excluded => IsAtOrBelow(normalizedChild, excluded));
                    if (!excludedDirectoryNames.Contains(name) && !isConfiguredExclude && !isReparsePoint)
                    {
                        pending.Push(child);
                    }
                }
            }
            claudeFiles.Sort(System.StringComparer.OrdinalIgnoreCase);

            var claudeSet = new System.Collections.Generic.HashSet<string>(claudeFiles, System.StringComparer.OrdinalIgnoreCase);
            foreach (var orphan in agentsFiles)
            {
                var siblingClaude = System.IO.Path.Combine(System.IO.Path.GetDirectoryName(orphan), "CLAUDE.md");
                if (!claudeSet.Contains(siblingClaude))
                {
                    Log.LogError("AGENTS.md has no sibling CLAUDE.md source: " + orphan);
                    success = false;
                }
            }

            const string generatedHeader = "> **Auto-generated from `CLAUDE.md`** — edit the sibling `CLAUDE.md` instead. Direct changes are overwritten by B44.Standards on the next synchronized build.";
            foreach (var source in claudeFiles)
            {
                bool sourceBom;
                string sourceText;
                if (string.Equals(source, rootClaude, System.StringComparison.OrdinalIgnoreCase))
                {
                    sourceText = updatedRoot;
                    sourceBom = rootBom;
                }
                else
                {
                    sourceText = ReadUtf8(source, out sourceBom);
                }
                var sourceNewline = NewlineFor(sourceText);
                var agentsText = System.Text.RegularExpressions.Regex.Replace(
                    sourceText,
                    @"`AGENTS\.md` files are auto-generated from sibling `CLAUDE\.md` by the opt-in `B44\.Standards` build target\. Edit the `CLAUDE\.md`, not the `AGENTS\.md`\.",
                    "`AGENTS.md` files are auto-generated on build; see the generated header for the source file to edit.",
                    System.Text.RegularExpressions.RegexOptions.IgnoreCase);
                agentsText = System.Text.RegularExpressions.Regex.Replace(
                    agentsText,
                    @"`AGENTS\.md` files are auto-generated from sibling `CLAUDE\.md` on build via `Directory\.Build\.targets`\. Edit the `CLAUDE\.md`, not the `AGENTS\.md`\.",
                    "`AGENTS.md` files are auto-generated on build; see the generated header for the source file to edit.",
                    System.Text.RegularExpressions.RegexOptions.IgnoreCase);
                agentsText = System.Text.RegularExpressions.Regex.Replace(
                    agentsText,
                    "CLAUDE\\.md",
                    "AGENTS.md",
                    System.Text.RegularExpressions.RegexOptions.IgnoreCase);
                var expectedAgents = generatedHeader + sourceNewline + sourceNewline + agentsText;
                var destination = System.IO.Path.Combine(System.IO.Path.GetDirectoryName(source), "AGENTS.md");
                success = WriteOrVerify(destination, expectedAgents, sourceBom, "Generated AGENTS.md") && success;
            }

            if (!success && !Log.HasLoggedErrors)
            {
                Log.LogError("B44 agent guidance synchronization did not complete successfully.");
            }
        }
        catch (System.Exception ex)
        {
            Log.LogErrorFromException(ex, true);
            return false;
        }
        ]]>
      </Code>
    </Task>
  </UsingTask>

  <!-- Source-size ratchet. Opt-in, and configured once per repository in
       Directory.Build.props so the gate and the regenerator read the SAME
       exclude list — a regenerator with its own copy of that list eventually
       writes a baseline the gate rejects.

         <B44RatchetEnabled>true</B44RatchetEnabled>
         <B44RatchetProject>...</B44RatchetProject>        anchor: runs once
         <B44RatchetRepositoryRoot>...</B44RatchetRepositoryRoot>
         <B44RatchetBaseline>...</B44RatchetBaseline>
         <B44RatchetExclude Include="MyGame.Tests" />      top-level dirs
         <B44RatchetExclude Include=".godot" />

       Verification runs on every build. Regeneration is a separate, explicit
       invocation that a normal build can never reach:

         dotnet build <anchor project> -t:B44WriteRatchetBaseline -->
  <PropertyGroup>
    <B44RatchetNewFileMaxLines Condition="'$(B44RatchetNewFileMaxLines)' == ''">500</B44RatchetNewFileMaxLines>
    <B44RatchetTrackThresholdLines Condition="'$(B44RatchetTrackThresholdLines)' == ''">350</B44RatchetTrackThresholdLines>
    <B44RatchetProjectFullPath Condition="'$(B44RatchetProject)' != ''">$([MSBuild]::NormalizePath('$(B44RatchetProject)'))</B44RatchetProjectFullPath>
  </PropertyGroup>

  <UsingTask
    TaskName="B44SourceSizeRatchetTask"
    TaskFactory="RoslynCodeTaskFactory"
    AssemblyFile="$(MSBuildToolsPath)/Microsoft.Build.Tasks.Core.dll">
    <ParameterGroup>
      <RepositoryRoot ParameterType="System.String" Required="true" />
      <BaselinePath ParameterType="System.String" Required="true" />
      <ExcludedDirectories ParameterType="Microsoft.Build.Framework.ITaskItem[]" Required="false" />
      <NewFileMaxLines ParameterType="System.Int32" Required="true" />
      <TrackThresholdLines ParameterType="System.Int32" Required="true" />
      <WriteBaseline ParameterType="System.Boolean" Required="true" />
      <!-- Both shipped targets pass FailOnViolation=true. It exists so the
           build-only fixture can assert ViolationCount without the fixture's
           own deliberate violations failing its build. It is NOT a consumer
           switch: reaching it means writing a target that bypasses the gate,
           which is a visible act, not a configuration tweak. -->
      <FailOnViolation ParameterType="System.Boolean" Required="true" />
      <ViolationCount ParameterType="System.Int32" Output="true" />
    </ParameterGroup>
    <Task>
      <Code Type="Fragment" Language="cs">
        <![CDATA[
        // net472-era MSBuild hosts lack Path.GetRelativePath. Declared first so
        // nothing follows the try/catch: RoslynCodeTaskFactory appends its own
        // return, and a fragment that always returns makes that unreachable.
        string GetRelativePath(string relativeTo, string path)
        {
            var baseUri = new System.Uri(
                relativeTo.EndsWith(System.IO.Path.DirectorySeparatorChar.ToString(), System.StringComparison.Ordinal)
                    ? relativeTo
                    : relativeTo + System.IO.Path.DirectorySeparatorChar);
            var target = new System.Uri(path);
            return System.Uri.UnescapeDataString(baseUri.MakeRelativeUri(target).ToString())
                .Replace('/', System.IO.Path.DirectorySeparatorChar);
        }

        void LogViolation(string message)
        {
            if (FailOnViolation) Log.LogError(message);
            else Log.LogMessage(Microsoft.Build.Framework.MessageImportance.High, message);
        }

        // Every failure path below logs an error rather than returning one.
        // MSBuild fails the task on any logged error, so the gate holds without
        // this fragment needing a return statement at all.
        try
        {
            var root = System.IO.Path.GetFullPath(RepositoryRoot);
            if (!System.IO.Directory.Exists(root))
            {
                Log.LogError("B44RatchetRepositoryRoot does not exist: " + root);
            }
            else
            {
            var excluded = new System.Collections.Generic.HashSet<string>(System.StringComparer.Ordinal);
            foreach (var item in ExcludedDirectories ?? System.Array.Empty<Microsoft.Build.Framework.ITaskItem>())
            {
                var name = (item.ItemSpec ?? string.Empty).Trim();
                if (name.Length > 0) excluded.Add(name.Replace('\\', '/').TrimEnd('/'));
            }

            // Enumerate production sources exactly as the ratchet always has:
            // recursive *.cs, never bin/obj at any depth, and skipping the
            // configured TOP-LEVEL directories (test projects exceed the
            // ratchet by doctrine; engine folders hold generated sources).
            var sources = new System.Collections.Generic.List<System.Collections.Generic.KeyValuePair<string, int>>();
            foreach (var file in System.IO.Directory.EnumerateFiles(root, "*.cs", System.IO.SearchOption.AllDirectories))
            {
                var relative = GetRelativePath(root, file).Replace('\\', '/');
                var parts = relative.Split('/');
                var skip = excluded.Contains(parts[0]);
                for (var i = 0; !skip && i < parts.Length; i++)
                {
                    if (parts[i] == "bin" || parts[i] == "obj") skip = true;
                }
                if (skip) continue;

                var lines = 0;
                foreach (var unused in System.IO.File.ReadLines(file)) lines++;
                sources.Add(new System.Collections.Generic.KeyValuePair<string, int>(relative, lines));
            }

            sources.Sort((a, b) => string.CompareOrdinal(a.Key, b.Key));

            if (WriteBaseline)
            {
                // Preserve the existing file's line-ending style. Baselines
                // differ across the portfolio (some LF, some CRLF) and a
                // regeneration that silently rewrites every line is
                // indistinguishable from one that changed a value.
                var newline = System.Environment.NewLine;
                // Carry each entry's trailing "# reason" across the rewrite.
                // Those comments are the record of WHY a file was granted an
                // exception; regenerating after an unrelated extraction must
                // not quietly erase the justification for every other entry.
                // (Standalone comment lines between entries are not preserved —
                // put a reason on the entry it belongs to.)
                var comments = new System.Collections.Generic.Dictionary<string, string>(System.StringComparer.Ordinal);
                if (System.IO.File.Exists(BaselinePath))
                {
                    var existing = System.IO.File.ReadAllText(BaselinePath);
                    newline = existing.IndexOf("\r\n", System.StringComparison.Ordinal) >= 0 ? "\r\n" : "\n";

                    foreach (var rawLine in System.IO.File.ReadLines(BaselinePath))
                    {
                        var hash = rawLine.IndexOf('#');
                        if (hash <= 0) continue;

                        var entryPart = rawLine.Substring(0, hash);
                        var equals = entryPart.IndexOf('=');
                        if (equals < 0) continue;

                        var entryPath = entryPart.Substring(0, equals).Trim().Replace('\\', '/');
                        if (entryPath.Length == 0) continue;

                        // Keep the original spacing before the '#' so a
                        // regeneration with no real change stays byte-identical.
                        var wsStart = hash;
                        while (wsStart > 0 && (entryPart[wsStart - 1] == ' ' || entryPart[wsStart - 1] == '\t')) wsStart--;
                        comments[entryPath] = rawLine.Substring(wsStart);
                    }
                }

                var builder = new System.Text.StringBuilder();
                builder.Append("# B44 source-size ratchet baseline. Files listed here exceed the").Append(newline);
                builder.Append("# ").Append(TrackThresholdLines)
                    .Append("-line tracking threshold and may not grow past their recorded size.").Append(newline);
                builder.Append("# Regenerated via: dotnet build <anchor project> -t:B44WriteRatchetBaseline").Append(newline);
                var recorded = 0;
                foreach (var source in sources)
                {
                    if (source.Value <= TrackThresholdLines) continue;
                    builder.Append(source.Key).Append(" = ").Append(source.Value);
                    string carriedComment;
                    if (comments.TryGetValue(source.Key, out carriedComment)) builder.Append(carriedComment);
                    builder.Append(newline);
                    recorded++;
                }

                var directory = System.IO.Path.GetDirectoryName(BaselinePath);
                if (!string.IsNullOrEmpty(directory)) System.IO.Directory.CreateDirectory(directory);
                System.IO.File.WriteAllText(BaselinePath, builder.ToString(), new System.Text.UTF8Encoding(false));
                Log.LogMessage(
                    Microsoft.Build.Framework.MessageImportance.High,
                    "Wrote ratchet baseline with " + recorded + " tracked file(s): " + BaselinePath);
            }
            else
            {
            var baseline = new System.Collections.Generic.Dictionary<string, int>(System.StringComparer.Ordinal);
            if (System.IO.File.Exists(BaselinePath))
            {
                foreach (var rawLine in System.IO.File.ReadLines(BaselinePath))
                {
                    var line = rawLine.Split('#')[0].Trim();
                    if (line.Length == 0) continue;

                    var parts = line.Split(new[] { '=' }, 2);
                    int allowed;
                    if (parts.Length != 2 || !int.TryParse(parts[1].Trim(), out allowed))
                    {
                        // Hard-fail rather than degrade to an empty baseline: a
                        // baseline that silently parses as "nothing tracked"
                        // disables the gate without anything looking wrong.
                        // Returned explicitly so the failure survives even when
                        // a caller downgrades errors with ContinueOnError.
                        Log.LogError(
                            null, "B44R001", null, null, 0, 0, 0, 0,
                            "Unreadable ratchet baseline entry in " + BaselinePath + ": '" + rawLine + "'");
                        return false;
                    }

                    baseline[parts[0].Trim().Replace('\\', '/')] = allowed;
                }
            }

            var violations = 0;
            foreach (var source in sources)
            {
                int allowed;
                if (baseline.TryGetValue(source.Key, out allowed))
                {
                    if (source.Value > allowed)
                    {
                        violations++;
                        LogViolation(
                            "B44 ratchet: " + source.Key + " is " + source.Value + " lines, past its baseline of " +
                            allowed + ". Extract a cohesive owner and leave the file smaller. Do not raise the " +
                            "baseline to fit: it moves only in a change that performs a real extraction.");
                    }
                }
                else if (source.Value > NewFileMaxLines)
                {
                    violations++;
                    LogViolation(
                        "B44 ratchet: " + source.Key + " is " + source.Value + " lines, over the " + NewFileMaxLines +
                        "-line limit for a new production file. Split it along a real seam.");
                }
            }

            ViolationCount = violations;
            if (violations > 0)
            {
                LogViolation(
                    "B44 ratchet: " + violations + " violation(s). Splitting a file in a way that leaves the code " +
                    "worse is NOT the fix — no cosmetic partial classes, one-method services, utility dumping " +
                    "grounds, or needless factories. If the file genuinely warrants an exception, stop and ask " +
                    "the repository maintainer; an agent must never grant itself one by editing the baseline or this configuration.");
            }
            }
            }
        }
        catch (System.Exception ex)
        {
            Log.LogErrorFromException(ex, true);
        }
        ]]>
      </Code>
    </Task>
  </UsingTask>

  <!-- Same anchor hazard as the hygiene checks, in the check they inherit
       their anchor from: B44VerifyRatchet compares B44RatchetProject against
       the project being built, so an empty anchor matches nothing and the
       ratchet silently never runs. Reported from every project rather than
       from an anchor that by definition is not there. Turn it off with
       B44VerifyRatchetAnchor=false. -->
  <Target Name="B44VerifyRatchetAnchor"
          BeforeTargets="PrepareForBuild"
          Condition="'$(B44VerifyRatchetAnchor)' != 'false' and '$(B44RatchetEnabled)' == 'true'">
    <Error Condition="'$(B44RatchetProject)' == ''"
           Code="B44R002"
           Text="B44RatchetEnabled is true but B44RatchetProject is empty, so the ratchet would match no project and never run. Set it in Directory.Build.props to the project that anchors the once-per-build checks, or remove B44RatchetEnabled." />
    <Error Condition="'$(B44RatchetProject)' != '' and !Exists('$(B44RatchetProjectFullPath)')"
           Code="B44R003"
           Text="B44RatchetProject is '$(B44RatchetProject)', which does not exist, so the ratchet would match no project and never run. Point it at a project that is built in this repository." />
  </Target>

  <Target Name="B44VerifyRatchet"
          BeforeTargets="PrepareForBuild"
          Condition="'$(B44RatchetEnabled)' == 'true' and '$(B44RatchetProjectFullPath)' == '$(MSBuildProjectFullPath)'">
    <Error Condition="'$(B44RatchetRepositoryRoot)' == ''"
           Text="B44RatchetEnabled requires B44RatchetRepositoryRoot. Set it to $(MSBuildThisFileDirectory) from the repository's Directory.Build.props." />
    <Error Condition="'$(B44RatchetBaseline)' == ''"
           Text="B44RatchetEnabled requires B44RatchetBaseline, the path to the checked-in ratchet baseline file." />
    <B44SourceSizeRatchetTask
      RepositoryRoot="$(B44RatchetRepositoryRoot)"
      BaselinePath="$(B44RatchetBaseline)"
      ExcludedDirectories="@(B44RatchetExclude)"
      NewFileMaxLines="$(B44RatchetNewFileMaxLines)"
      TrackThresholdLines="$(B44RatchetTrackThresholdLines)"
      WriteBaseline="false"
      FailOnViolation="true" />
  </Target>

  <!-- Deliberately NOT hooked to any build target. Regenerating the baseline is
       an explicit act performed in the same change as a real extraction; a
       gate that can rewrite its own expectations during an ordinary build is
       not a gate. -->
  <Target Name="B44WriteRatchetBaseline">
    <Error Condition="'$(B44RatchetEnabled)' != 'true'"
           Text="B44WriteRatchetBaseline requires B44RatchetEnabled=true in this repository's Directory.Build.props." />
    <Error Condition="'$(B44RatchetRepositoryRoot)' == '' or '$(B44RatchetBaseline)' == ''"
           Text="B44WriteRatchetBaseline requires B44RatchetRepositoryRoot and B44RatchetBaseline." />
    <B44SourceSizeRatchetTask
      RepositoryRoot="$(B44RatchetRepositoryRoot)"
      BaselinePath="$(B44RatchetBaseline)"
      ExcludedDirectories="@(B44RatchetExclude)"
      NewFileMaxLines="$(B44RatchetNewFileMaxLines)"
      TrackThresholdLines="$(B44RatchetTrackThresholdLines)"
      WriteBaseline="true"
      FailOnViolation="true" />
  </Target>

  <Target Name="B44SyncAgentGuidance"
          BeforeTargets="PrepareForBuild"
          Condition="'$(B44AgentSyncEnabled)' == 'true' and '$(B44AgentSyncProjectFullPath)' == '$(MSBuildProjectFullPath)'">
    <Error Condition="'$(B44AgentRepositoryRoot)' == ''"
           Text="B44AgentSyncEnabled requires B44AgentRepositoryRoot. Set it to $(MSBuildThisFileDirectory) from the repository's Directory.Build.props." />
    <Error Condition="'$(B44AgentGuidanceProfile)' == ''"
           Text="B44AgentSyncEnabled requires B44AgentGuidanceProfile=Organization or Game." />
    <Error Condition="'$(B44AgentGuidanceProfile)' == 'Game' and '$(B44GameCoreProject)' == ''"
           Text="The B44 Game guidance profile requires B44GameCoreProject to identify the mandatory engine-free Core project." />
    <Error Condition="'$(B44AgentGuidanceProfile)' == 'Game' and '$(B44GameCoreProject)' != '' and !Exists('$(B44GameCoreProject)')"
           Text="The B44 Game guidance profile requires an existing engine-free Core project. B44GameCoreProject was '$(B44GameCoreProject)'." />
    <Error Condition="'$(B44AgentGuidanceProfile)' == 'Game' and '$(B44AgentSyncProjectFullPath)' != '$(B44GameCoreProjectFullPath)'"
           Text="The B44 Game guidance profile requires its engine-free Core project to be the synchronization anchor. Set B44AgentSyncProject=$(B44GameCoreProject)." />
    <Error Condition="'$(B44AgentGuidanceProfile)' == 'Game' and '$(B44EngineFreeCore)' != 'true'"
           Text="The B44 Game guidance profile requires the anchor Core project to set B44EngineFreeCore=true." />
    <Message Text="Synchronizing B44 $(B44AgentGuidanceProfile) guidance and CLAUDE.md/AGENTS.md mirrors."
             Importance="High" />
    <B44SyncAgentGuidanceTask
      RepositoryRoot="$(B44AgentRepositoryRoot)"
      GuidanceProfile="$(B44AgentGuidanceProfile)"
      OrganizationGuidancePath="$(MSBuildThisFileDirectory)..\guidance\B44.Organization.md"
      GameGuidancePath="$(MSBuildThisFileDirectory)..\guidance\B44.Game.md"
      ToolingGuidancePath="$(MSBuildThisFileDirectory)..\guidance\B44.Tooling.md"
      ExcludedPaths="@(B44AgentSyncExclude)"
      VerifyOnly="$(B44AgentSyncVerifyOnly)" />
  </Target>
</Project>
